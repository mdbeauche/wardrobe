import Counter from '../components/Counter/Counter';

const Sandbox = () => (
  <div className="App">
    <header className="App-header">
      <Counter />
    </header>
  </div>
);

export default Sandbox;
